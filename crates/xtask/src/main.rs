// TEMPORARY J1 scaffold, replaced at integration.
// The real tasks (I02 scan, conformance driver) are written by the fixture slice.

fn main() {
    eprintln!("xtask: scaffold only; no task is implemented");
    std::process::exit(3);
}
