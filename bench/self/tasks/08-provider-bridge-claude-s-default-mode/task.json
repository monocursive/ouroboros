{
  "id": "08-provider-bridge-claude-s-default-mode",
  "base_sha": "b564417c7099f1946ad2c62a31691902498618c2",
  "commit_sha": "9ff73a5f61b106e8b0912d1e5b44a3ca5d27047f",
  "subject": "fix(provider): bridge Claude's :default mode too, since its own default asks",
  "instruction": "fix(provider): bridge Claude's :default mode too, since its own default asks\n\nC2 attached the permission-prompt bridge only at approval_mode: :prompt,\nleaving :default — Claude Code's own default permission mode, which asks\nbefore writes and under --print can only ask through a prompt tool — as\nthe silent denial X1 was written to end. Bridge both; :auto_edit and\n:auto_approve stay byte-identical to the pinned adapter's argv.\n\nAcceptance. These tests are restored into the worktree after the session ends and must pass:\n  - :default is bridged like :prompt, because Claude's own default mode asks\n  - the two auto modes are left exactly as they were\n\nRules. Files that already exist under `test/` must not be modified or deleted — the change is graded against tests restored from the history after the session ends, applied to a fresh checkout of the starting commit. New test files of your own are allowed and are left in place, but they are yours: they are not copied into the tree the grade runs in. The change is collected as a diff over `lib/`, `assets/`, `priv/`, `config/`, `docs/` and `README.md`; a change to any other path — `mix.exs` and `.formatter.exs` included — is refused rather than graded. Work only inside this worktree.",
  "instruction_kind": "full",
  "hidden_tests": [
    "test/provider/claude_adapter_test.exs"
  ],
  "solution_files": [
    "README.md",
    "lib/ouroboros/provider/claude_adapter.ex"
  ],
  "timeout_secs": 600,
  "measured": {"compile_ms": 54605, "diff_lines": 14, "test_ms": 3770}
}
